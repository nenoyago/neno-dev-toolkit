import { RouteTransformDirective } from './route-transform.directive';

describe('RouteTransformDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteTransformDirective();
    expect(directive).toBeTruthy();
  });
});
